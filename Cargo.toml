[workspace]
resolver = "2"
members = [
  "crates/page_cli",
  "crates/page_validation",
]

[workspace.package]
version = "0.1.3"
edition = "2024"
license = "MIT"

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
