{"config":{"lang":["en"],"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"page","text":"<p>A fast and lightweight PDF accessibility and compliance checker. You can currently use it as a CLI, a Rust crate and a Python package.</p>","path":["Home"],"tags":[]},{"location":"#features","level":2,"title":"Features","text":"<ul> <li>Accessibility: check PDFs against PDF/UA-1 accessibility requirements</li> <li>PDF/A compliance: validate PDF/A-1, PDF/A-2, and PDF/A-3 documents</li> <li>Tested: uses the same test corpus as veraPDF </li> <li>Fast: preliminary tests suggest that <code>page</code> is 3x to 15x faster than veraPDF</li> <li>Lightweight: ~9 MB binary, with peak RSS 3x to 8x lower than veraPDF</li> <li>Easy to use: ships as a single binary with zero runtime requirements, and can be used from the command line, Rust or Python</li> </ul> <p>Ready to get started? Check out installation instructions.</p>","path":["Home"],"tags":[]},{"location":"#roadmap","level":2,"title":"Roadmap","text":"<p>So far, all PDF/A and PDF/UA-1 formats have been implemented. The current focus is on consolidating those, making sure they are heavily tested against a large corpus of documents. The goal is to have those foundations completed by the end of 2026.</p>","path":["Home"],"tags":[]},{"location":"about/","level":1,"title":"About","text":"","path":["About"],"tags":[]},{"location":"about/#what-does-page-do","level":2,"title":"What does <code>page</code> do?","text":"<p><code>page</code> is, at its core, a PDF validation engine. In more practical terms, it verifies that a given PDF file complies with certain standards. For example, for a PDF to be considered accessible, it is often required to be PDF/UA-1 compliant.</p> <p>Since the creation of the PDF format more than 30 years ago, many different variants have appeared, each with a different purpose. For example, all PDF/A formats (around 10) focus on archival preservation, ensuring that a PDF created in the 2000s will still open decades from now, while PDF/UA formats (UA-1 and UA-2) focus on accessibility, ensuring that everyone, including people with visual disabilities, can use a PDF.</p> <p><code>page</code> aims to make that verification easy, fast, and free.</p>","path":["About"],"tags":[]},{"location":"about/#how-it-works","level":2,"title":"How it works?","text":"<p>When you give <code>page</code> a PDF, it reads the file and parses its structure into a normalized document model. This model brings together the PDF object graph and the information needed for validation, such as metadata, XMP declarations, output intents, fonts, pages, annotations, forms, and content streams. Parsing is done via lopdf, and is kept separate from conformance checks, so malformed input or resource-limit failures can be reported distinctly from a PDF that simply violates a standard.</p> <p>Next, <code>page</code> selects the validation profile: it uses the profile passed on the command line, or infers one from the PDF's XMP identification metadata when no profile is specified. A document is compliant only when every rule implemented for the selected profile passes.</p> <p>Internally, <code>page</code> has a \"fail fast\" mode, which lets us be around 2x to 5x faster depending on the document. Instead of checking for every possible rule, it stops at the very first one that is violated, and only all rules checked when it's required. This feature is automatically used when possible, and only relevant when the document isn't compliant.</p>","path":["About"],"tags":[]},{"location":"about/#what-are-the-alternatives","level":2,"title":"What are the alternatives?","text":"<p>There are multiple projects that attempt to provide a PDF validator, but only one is:</p> <ul> <li>open source</li> <li>based on ISO standards</li> <li>cross-platform</li> <li>non-commercial</li> </ul> <p>This project is veraPDF. It is, by far, the best option available out there. Other options such as PAC or Adobe Acrobat meet only a subset of those criteria.</p> <p><code>veraPDF</code> supports validation of all PDF/A (long-term archiving) and PDF/UA (universal accessibility) formats. It might not seem so, but this is a very complicated task. You can understand why that is and how <code>veraPDF</code> does it in this great blog post.</p> <p><code>page</code> uses heavily <code>veraPDF</code> as the source of truth to validate or not if what <code>page</code> does is correct.</p>","path":["About"],"tags":[]},{"location":"about/#why-are-there-multiple-pdf-formats","level":2,"title":"Why are there multiple PDF formats?","text":"<p>The PDF specification is intentionally broad: it supports features ranging from interactive forms and multimedia to encryption, JavaScript, and digital signatures. This flexibility makes PDF suitable for many use cases, but it also makes it impossible to guarantee that every PDF will behave consistently across all software and over long periods of time.</p> <p>To address this, the ISO standard defines a number of specialized PDF profiles. Each profile restricts or requires certain features to guarantee a particular property.</p> <p>Some of the most common profiles are:</p> <ul> <li><code>PDF/A</code>: Long-term document preservation and archiving.</li> <li><code>PDF/UA</code>: Universal accessibility for users of assistive technologies.</li> <li><code>PDF/X</code>: Reliable printing and graphic arts workflows.</li> <li><code>PDF/E</code>: Engineering and technical documentation.</li> <li><code>PDF/VT</code>: Variable-data and transactional printing.</li> </ul> <p>These profiles are not competing formats. Instead, they are subsets (and sometimes supersets) of the PDF specification, each designed for a specific purpose.</p> <p>For example, PDF/A forbids features such as JavaScript or external dependencies because they could make a document impossible to reproduce decades later. Conversely, PDF/UA requires additional semantic information (such as a tag tree and alternative text) to ensure documents are accessible to screen readers.</p> <p>Because each profile defines hundreds of requirements, validating a PDF is much more involved than simply checking whether it \"opens correctly.\" A validator must verify that every applicable rule is satisfied for the requested profile.</p>","path":["About"],"tags":[]},{"location":"benchmark/","level":1,"title":"Benchmark","text":"<p>Mean validation speedups are based on timings measured in milliseconds and normalized per document to veraPDF = 1.0×. Higher values are faster. Each value uses 10 runs (with 2 warmup runs) per validator and document.</p> <p>The 5 documents have varying size, content and method of creation. We're working on making the benchmark fully reproducible and share the documents used.</p> Document veraPDF page page fail-fast document1.pdf 1.0× 7.0× 17.5× document2.pdf 1.0× 7.9× 19.3× document3.pdf 1.0× 2.8× 6.4× document4.pdf 1.0× 3.8× 31.2× document5.pdf 1.0× 1.9× 28.3× <p>Note</p> <p>The fail fast mode of <code>page</code> (used automatically when possible) allows to get much faster results, but does not give details about which specific rules failed.</p>","path":["Benchmark"],"tags":[]},{"location":"installation/","level":1,"title":"Installation","text":"<p><code>page</code> distribute pre-built binaries for macOS/Linux/Windows. If you're a Rust hacker, you can also install the latest dev version via Cargo.</p> <p>Info</p> <p>Those are instructions for installing the CLI. If you want to integrate <code>page</code> in your Rust workflows, check out the page crate.</p>","path":["Installation"],"tags":[]},{"location":"installation/#macoslinux","level":3,"title":"macOS/Linux","text":"<pre><code>curl --proto '=https' --tlsv1.2 -LsSf https://github.com/josephbarbierdarnal/page/releases/download/v0.4.0/page_cli-installer.sh | sh\n</code></pre>","path":["Installation"],"tags":[]},{"location":"installation/#powershell","level":3,"title":"PowerShell","text":"<pre><code>powershell -ExecutionPolicy Bypass -c \"irm https://github.com/josephbarbierdarnal/page/releases/download/v0.4.0/page_cli-installer.ps1 | iex\"\n</code></pre>","path":["Installation"],"tags":[]},{"location":"installation/#cargo","level":3,"title":"Cargo","text":"<pre><code>cargo install --git https://github.com/josephbarbierdarnal/page.git page_cli --bin page\n</code></pre>","path":["Installation"],"tags":[]},{"location":"api/cli/","level":1,"title":"CLI","text":"<p>Info</p> <p>Check out the dedicated page for install instructions.</p> <p></p> <p>Validate one PDF (by default against the profile declared in its XMP metadata):</p> <pre><code>page document.pdf\n</code></pre> <pre><code>Result  : Conformant\nProfile : PDF/A-1b\nTime    : 0.005s\n</code></pre> <p>If the document does not declare a profile, <code>page</code> exits with an explicit error. Use <code>--profile</code> to select a profile instead:</p> <pre><code>page document.pdf --profile ua1\n</code></pre> <pre><code>Result  : Non-conformant\nProfile : PDF/UA-1\nTime    : 0.005s\n</code></pre> <p></p> <p>Add <code>--format details</code> to emit details about the failure:</p> <pre><code>page document.pdf --format details\n</code></pre> <pre><code>Result  : Non-conformant\nProfile : PDF/A-1b\nTime    : 0.007s\n\n[PDFA1B-HEADER-BINARY-COMMENT-001] Conformance: [.........]\n[PDFA1B-HEX-STRING-CHARACTERS-001] Conformance: [.........]\n[PDFA1B-ID-SCHEMA-001] Metadata: [.........]\n[PDFA1B-INFO-AUTHOR-001] Metadata: [.........]\n[PDFA1B-INFO-CREATOR-001] Metadata: [.........]\n[PDFA1B-INFO-KEYWORDS-001] Metadata: [.........]\n[PDFA1B-INFO-PRODUCER-001] Metadata: [.........]\n[PDFA1B-INFO-SUBJECT-001] Metadata: [.........]\n[PDFA1B-INFO-TITLE-001] Metadata: [.........]\n[PDFA1B-METADATA-STRUCTURE-001] Metadata: [.........]\n[PDFA1B-TRAILER-ID-001] Conformance: [.........]\n</code></pre> <p>Note</p> <p>[ . . . . . . . . . ] are just placeholders of the actual messages</p> <p></p> <p>Or use <code>--format json</code> to emit the details as JSON:</p> <pre><code>page document.pdf --format json\n</code></pre> <pre><code>{\n  \"file\": \"document.pdf\",\n  \"profile\": \"1b\",\n  \"valid\": false,\n  \"failures\": [\n    {\n      \"rule\": \"PDFA1B-TRAILER-ID-001\",\n      \"message\": \"the applicable document trailer does not contain an ID entry\"\n    },\n    {\n      \"rule\": \"PDFA1B-STREAM-LZW-001\",\n      \"message\": \"a parsed stream declares the forbidden LZWDecode filter\"\n    }\n  ]\n}\n</code></pre> <p></p> <p>Write the report to a file with <code>--output</code>. A <code>.json</code> extension selects JSON automatically; use <code>--format details</code> for a detailed text report:</p> <pre><code>page document.pdf --output report.json\npage document.pdf --format details --output report.txt\n</code></pre> <p>Explicit formats that conflict with <code>.json</code> or <code>.txt</code> are rejected. Other extensions, including no extension, are allowed. File output is uncolored and leaves stdout empty.</p>","path":["Usage","CLI"],"tags":[]},{"location":"api/cli/#colors","level":2,"title":"Colors","text":"<p>By default, <code>page</code> uses color in the terminal output.</p> <p></p> <p>In order to follow the NO_COLOR standard, you can either set the <code>NO_COLOR</code> environment variable to 1 or pass <code>--no-color</code> to disable them.</p>","path":["Usage","CLI"],"tags":[]},{"location":"api/python/","level":1,"title":"Python","text":"<p>The <code>page-validation</code> package provides Python bindings for PDF/A and PDF/UA validation. This page contains the most common usages; see the Python bindings repository for reference documentation.</p>","path":["Usage","Python"],"tags":[]},{"location":"api/python/#add-the-dependency","level":2,"title":"Add the dependency","text":"uvpip <pre><code>uv add page-validation\n</code></pre> <pre><code>pip install page-validation\n</code></pre>","path":["Usage","Python"],"tags":[]},{"location":"api/python/#check-compliance-of-a-pdf","level":2,"title":"Check compliance of a PDF","text":"<p><code>is_pdf_compliant()</code> is the fastest way to get a simple true/false compliance result for a profile. It returns both the selected profile and the boolean result:</p> <pre><code>import page\n\nis_compliant: bool = page.is_pdf_compliant(\"file.pdf\")\n</code></pre> <p>If the profile isn't specified, it reads the PDF/A or PDF/UA profile declared in the document's XMP metadata. A missing, malformed, or unsupported profile declaration, or an input that cannot be read or parsed, raises <code>page.ValidationError</code>.</p> <p>Info</p> <p>If you want to run it on bytes instead of a file, use <code>is_pdf_compliant_bytes()</code>, which provides the same API but expects a <code>bytes</code> value instead of a path.</p>","path":["Usage","Python"],"tags":[]},{"location":"api/python/#validate-a-pdf-with-details","level":2,"title":"Validate a PDF with details","text":"<p>If you need details about which rules failed, use <code>validate_pdf()</code>:</p> <pre><code>import page\n\nreport = page.validate_pdf(\"document.pdf\")\n\nif report.is_compliant:\n    print(\"The document passed all implemented checks.\")\nelse:\n    for failure in report.failures:\n        print(f\"[{failure.rule_id}] {failure.message}\")\n</code></pre> <p>Info</p> <p>If you want to run it on bytes instead of a file, use <code>validate_pdf_bytes()</code>, which provides the same API but expects a <code>bytes</code> value instead of a path.</p>","path":["Usage","Python"],"tags":[]},{"location":"api/python/#select-a-profile-explicitly","level":2,"title":"Select a profile explicitly","text":"<p>Pass a profile to <code>validate_pdf()</code> when the caller, rather than the document, selects it:</p> <pre><code>import page\n\nreport = page.validate_pdf(\n    \"document.pdf\",\n    page.ValidationProfile.PDF_A_1B,\n)\n</code></pre> <p>The explicit-profile call does not require the document to contain a usable profile declaration. The declaration can still fail the selected profile's metadata rules. Use <code>is_pdf_compliant()</code> or the corresponding bytes function when you only need a boolean result.</p>","path":["Usage","Python"],"tags":[]},{"location":"api/python/#failures","level":2,"title":"Failures","text":"<p>Each report contains a list of failures:</p> <pre><code>import page\n\nreport = page.validate_pdf(\"file.pdf\")\n\nfor failure in report.failures:\n    print(f\"Rule: {failure.rule_id}\")\n    print(f\"Category: {failure.category}\")\n    print(f\"Message: {failure.message}\")\n</code></pre> <p>Failure categories distinguish conformance problems from parser or operational errors:</p> <pre><code>import page\n\nfor failure in report.failures:\n    if failure.category in (\n        page.FailureCategory.METADATA,\n        page.FailureCategory.CONFORMANCE,\n    ):\n        # The PDF was parsed, but failed a validation rule.\n        pass\n    elif failure.category == page.FailureCategory.PARSER:\n        # The PDF could not be parsed correctly.\n        pass\n    elif failure.category == page.FailureCategory.OPERATIONAL:\n        # Validation failed because of I/O or another runtime issue.\n        pass\n</code></pre>","path":["Usage","Python"],"tags":[]},{"location":"api/python/#safety-limits","level":2,"title":"Safety limits","text":"<p>Safety limits protect the validator from excessively large or complex inputs. Defaults are sufficient for most cases:</p> <pre><code>import page\n\nlimits = page.SafetyLimits(\n    max_input_size=256 * 1024 * 1024,                 # 256 MiB\n    max_decoded_stream_size=32 * 1024 * 1024,         # 32 MiB\n    max_total_decoded_content_size=256 * 1024 * 1024, # 256 MiB\n    max_object_count=1_000_000,                       # 1,000,000 objects\n    max_reference_depth=256,                          # 256 levels\n    max_xref_revisions=1_024,                         # 1,024 revisions\n)\n\nreport = page.validate_pdf(\"document.pdf\", limits=limits)\n</code></pre> <p><code>max_decoded_stream_size</code> bounds one decoded stream and <code>max_total_decoded_content_size</code> bounds the total decoded page, Form, appearance, Pattern, and Type3 content inspected for one document. <code>max_xref_revisions</code> bounds the number of incremental-update revisions read from the cross-reference chain.</p>","path":["Usage","Python"],"tags":[]},{"location":"api/python/#use-the-exit-code","level":2,"title":"Use the exit code","text":"<p>For command-line integrations or automated checks, a report can provide an appropriate process exit code:</p> <pre><code>import sys\nimport page\n\nreport = page.validate_pdf(\"document.pdf\")\nsys.exit(report.exit_code())\n</code></pre> <p>The exit code is <code>0</code> for a compliant report, <code>2</code> for a noncompliant report, and <code>1</code> when the report contains an operational failure.</p>","path":["Usage","Python"],"tags":[]},{"location":"api/python/#export-the-report","level":2,"title":"Export the report","text":"<p>Validation reports can be exported as JSON:</p> <pre><code>import page\n\nreport = page.validate_pdf(\"document.pdf\")\n\nwith open(\"report.json\", \"w\") as output:\n    output.write(report.to_json())\n</code></pre>","path":["Usage","Python"],"tags":[]},{"location":"api/rust/","level":1,"title":"Rust","text":"<p>The <code>page_validation</code> crate validates PDF files against a supported PDF/A or PDF/UA profile. This page contains most common usages, but you can find reference documentation at docs.rs.</p>","path":["Usage","Rust"],"tags":[]},{"location":"api/rust/#add-the-dependency","level":2,"title":"Add the dependency","text":"<p>Either run:</p> <pre><code>cargo add page_validation\n</code></pre> <p>Or add the following to your <code>Cargo.toml</code>:</p> <pre><code>[dependencies]\npage_validation = \"0.6.1\"\n</code></pre>","path":["Usage","Rust"],"tags":[]},{"location":"api/rust/#check-compliance-of-a-pdf","level":2,"title":"Check compliance of a PDF","text":"<p><code>is_pdf_compliant()</code> is the fastest way to get a simple true/false compliance result against a profile. It stops once it finds a failing rule and returns the boolean directly:</p> <pre><code>use std::path::Path;\nuse page_validation::{ValidationProfile, SafetyLimits, is_pdf_compliant};\n\nlet is_compliant = is_pdf_compliant(\n    Path::new(\"file.pdf\"),            // path to a PDF\n    Some(ValidationProfile::PdfUA1),  // an optional profile\n    &amp;SafetyLimits::default(),         // see below\n)?;\nprintln!(\"{is_compliant}\");\n</code></pre> <p>If the profile isn't specified, it reads the PDF/A or PDF/UA profile declared in the document's XMP metadata and returns <code>Result&lt;bool, ValidationError&gt;</code>. A missing, malformed, or unsupported profile declaration produces a <code>ValidationError</code>. Also see, safety limits.</p> <p>Info</p> <p>If you want to run it on bytes instead of a file, use <code>is_pdf_compliant_bytes()</code>, which provides the same <code>Result&lt;bool, ValidationError&gt;</code> API but expects a <code>&amp;[u8]</code> instead of a <code>&amp;Path</code>.</p>","path":["Usage","Rust"],"tags":[]},{"location":"api/rust/#validate-a-pdf-with-details","level":2,"title":"Validate a PDF with details","text":"<p>If you details about which rule failed, use <code>validate_pdf()</code>:</p> <pre><code>use std::path::Path;\nuse page_validation::{SafetyLimits, validate_pdf};\n\nlet doc = Path::new(\"document.pdf\")\nlet report = validate_pdf(doc, None, &amp;SafetyLimits::default())?;\n\nif report.is_compliant {\n    println!(\"The document passed all implemented checks.\");\n} else {\n    for failure in &amp;report.failures {\n        eprintln!(\n            \"[{}] {}\",\n            failure.rule_id,\n            failure.message,\n        );\n    }\n}\n</code></pre> <p>Info</p> <p>If you want to run it on bytes instead of a file, use <code>validate_pdf_bytes()</code>, which provides the same API but expects a <code>&amp;[u8]</code> instead of a <code>&amp;Path</code>.</p>","path":["Usage","Rust"],"tags":[]},{"location":"api/rust/#select-a-profile-explicitly","level":2,"title":"Select a profile explicitly","text":"<p>Pass a profile to <code>validate_pdf()</code> when the caller, rather than the document, selects it:</p> <pre><code>use std::path::Path;\nuse page_validation::{SafetyLimits, ValidationProfile, validate_pdf};\n\nlet report = validate_pdf(\n    Path::new(\"document.pdf\"),\n    Some(ValidationProfile::PdfA1b),\n    &amp;SafetyLimits::default(),\n);\n</code></pre> <p>The explicit-profile call returns <code>Result&lt;ValidationReport, ValidationError&gt;</code>. Unlike profile inference, it does not require the document to contain a usable profile declaration. The declaration can still fail the selected profile's metadata rules.</p>","path":["Usage","Rust"],"tags":[]},{"location":"api/rust/#failures","level":2,"title":"Failures","text":"<p>Each report contains a list of failures:</p> <pre><code>use std::path::Path;\nuse page_validation::{SafetyLimits, validate_pdf};\n\nlet report = validate_pdf(\n    Path::new(\"file.pdf\"),\n    None,\n    &amp;SafetyLimits::default()\n)?;\n\nfor failure in &amp;report.failures {\n    println!(\"Rule: {}\", failure.rule_id);\n    println!(\"Category: {:?}\", failure.category);\n    println!(\"Message: {}\", failure.message);\n}\n</code></pre> <p>Failure categories distinguish conformance problems from parser or operational errors:</p> <pre><code>use page_validation::FailureCategory;\n\nmatch failure.category {\n    FailureCategory::Metadata | FailureCategory::Conformance =&gt; {\n        // The PDF was parsed, but failed a validation rule.\n    }\n    FailureCategory::Parser =&gt; {\n        // The PDF could not be parsed correctly.\n    }\n    FailureCategory::Operational =&gt; {\n        // Validation failed because of I/O or another runtime issue.\n    }\n}\n</code></pre>","path":["Usage","Rust"],"tags":[]},{"location":"api/rust/#safety-limits","level":2,"title":"Safety limits","text":"<p>The goal of the safety limits protect the validator from excessively large or complex inputs. Defaults are the following and should be sufficient for most cases:</p> <pre><code>use page_validation::SafetyLimits;\n\nlet limits = SafetyLimits {\n    max_input_size: 100 * 1024 * 1024,                 // 100 MiB\n    max_decoded_stream_size: 32 * 1024 * 1024,         // 32 MiB\n    max_total_decoded_content_size: 100 * 1024 * 1024, // 100 MiB\n    max_object_count: 500_000,                         // 500,000 objects\n    max_reference_depth: 256,                          // 256 levels\n};\n</code></pre> <p><code>max_decoded_stream_size</code> bounds one decoded stream and <code>max_total_decoded_content_size</code> bounds the total decoded page, Form, appearance, Pattern, and Type3 content inspected for one document.</p>","path":["Usage","Rust"],"tags":[]},{"location":"api/rust/#use-the-exit-code","level":2,"title":"Use the exit code","text":"<p>For command-line integrations or automated checks, the report can provide an appropriate process exit code:</p> <pre><code>std::process::exit(report.exit_code());\n</code></pre>","path":["Usage","Rust"],"tags":[]},{"location":"contributing/","level":1,"title":"Contributing","text":"<p>Thanks for wanting to contribute to <code>page</code>!</p>","path":["Contributing"],"tags":[]},{"location":"contributing/code-of-conduct/","level":1,"title":"Code of conduct","text":"","path":["Contributing","Code of conduct"],"tags":[]},{"location":"contributing/code-of-conduct/#our-commitment","level":2,"title":"Our Commitment","text":"<p>We are committed to providing a welcoming, respectful, and harassment-free environment for everyone who participates in this project.</p> <p>We expect all contributors, maintainers, users, and other community members to treat each other with respect and to help keep discussions constructive.</p>","path":["Contributing","Code of conduct"],"tags":[]},{"location":"contributing/code-of-conduct/#expected-behavior","level":2,"title":"Expected Behavior","text":"<p>Participants should:</p> <ul> <li>Be respectful and considerate of others.</li> <li>Give and receive technical feedback constructively.</li> <li>Focus disagreements on ideas, code, and decisions rather than individuals.</li> <li>Respect differing viewpoints, experiences, and levels of expertise.</li> <li>Assume good faith while remaining open to correction.</li> <li>Help create an environment where people feel comfortable asking questions and contributing.</li> </ul>","path":["Contributing","Code of conduct"],"tags":[]},{"location":"contributing/code-of-conduct/#unacceptable-behavior","level":2,"title":"Unacceptable Behavior","text":"<p>The following behavior is not acceptable:</p> <ul> <li>Harassment, intimidation, threats, or discrimination.</li> <li>Personal attacks, insults, or deliberately hostile comments.</li> <li>Sexualized language, imagery, or unwanted sexual attention.</li> <li>Trolling or repeatedly disrupting productive discussions.</li> <li>Publishing another person's private information without permission.</li> <li>Deliberately targeting or excluding individuals in a way that makes participation unsafe or hostile.</li> <li>Other conduct that would reasonably be considered inappropriate in a professional or collaborative environment.</li> </ul>","path":["Contributing","Code of conduct"],"tags":[]},{"location":"contributing/code-of-conduct/#scope","level":2,"title":"Scope","text":"<p>This Code of Conduct applies to all project spaces, including issues, pull requests, discussions, code reviews, documentation, and other communication related to the project.</p> <p>It also applies when someone is officially representing the project in public spaces.</p>","path":["Contributing","Code of conduct"],"tags":[]},{"location":"contributing/code-of-conduct/#enforcement","level":2,"title":"Enforcement","text":"<p>Project maintainers are responsible for interpreting and enforcing this Code of Conduct.</p> <p>Maintainers may edit or remove comments, commits, issues, pull requests, discussions, or other contributions that violate these standards. Depending on the circumstances, maintainers may issue a warning, temporarily restrict participation, or permanently ban a participant from project spaces.</p> <p>Enforcement decisions should be proportionate to the nature and severity of the behavior.</p>","path":["Contributing","Code of conduct"],"tags":[]},{"location":"contributing/code-of-conduct/#reporting","level":2,"title":"Reporting","text":"<p>If you experience or observe behavior that violates this Code of Conduct, report it privately to the project maintainers through the project's designated contact method.</p> <p>Reports will be reviewed as promptly and fairly as reasonably possible. Maintainers should respect the privacy and safety of everyone involved when handling a report.</p>","path":["Contributing","Code of conduct"],"tags":[]},{"location":"contributing/code-of-conduct/#good-faith-participation","level":2,"title":"Good-Faith Participation","text":"<p>Disagreement is a normal part of open-source development. Criticism of code, technical decisions, or proposals is welcome when it is relevant and constructive.</p> <p>This Code of Conduct is intended to protect productive participation, not to prevent reasonable technical debate or good-faith differences of opinion.</p>","path":["Contributing","Code of conduct"],"tags":[]},{"location":"contributing/local-set-up/","level":1,"title":"Local set up","text":"<p>Thanks for taking the time to improve <code>page</code>. This guide covers the local setup and the main checks to run before opening a pull request.</p> <p>Before making changes, comment on an existing issue or open a new one.</p>","path":["Contributing","Local set up"],"tags":[]},{"location":"contributing/local-set-up/#fork-and-clone","level":2,"title":"Fork and clone","text":"<p>Fork the repository on GitHub, then clone your fork locally:</p> <pre><code>git clone git@github.com:&lt;your-username&gt;/page.git\ncd page\ngit remote add upstream https://github.com/josephbarbierdarnal/page.git\ngit fetch upstream\n</code></pre> <p>Create a branch for your change:</p> <pre><code>git checkout -b &lt;short-description&gt;\n</code></pre>","path":["Contributing","Local set up"],"tags":[]},{"location":"contributing/local-set-up/#prerequisites","level":2,"title":"Prerequisites","text":"<p>Install these tools before setting up the project:</p> <ul> <li><code>Git</code></li> <li><code>Cargo</code> and Rust &gt;= 1.88.0</li> <li><code>uv</code> if you want to preview the documentation website</li> <li>(optional but recommended) <code>just</code> for running project tasks, see <code>justfile</code> file</li> </ul>","path":["Contributing","Local set up"],"tags":[]},{"location":"contributing/local-set-up/#install-verapdf","level":2,"title":"Install veraPDF","text":"<p>Download the veraPDF 1.30.2 Greenfield installer, extract it, and run <code>verapdf-install</code> on macOS/Linux or <code>verapdf-install.bat</code> on Windows.</p> <p>Put the installed CLI on <code>PATH</code>, or point the project to it explicitly:</p> <pre><code>export VERAPDF_BIN=/path/to/verapdf/bin/verapdf\n\"$VERAPDF_BIN\" --version\n</code></pre> <p>The version must be 1.30.x; 1.30.2 is recommended.</p>","path":["Contributing","Local set up"],"tags":[]},{"location":"contributing/local-set-up/#install-dependencies","level":2,"title":"Install dependencies","text":"<p>Fetch the locked Rust dependencies:</p> <pre><code>cargo fetch --locked\n</code></pre>","path":["Contributing","Local set up"],"tags":[]},{"location":"contributing/local-set-up/#run-unit-tests","level":2,"title":"Run unit tests","text":"<p>Run the full workspace test suite:</p> <pre><code>just test\n</code></pre>","path":["Contributing","Local set up"],"tags":[]},{"location":"contributing/local-set-up/#run-corpus-tests","level":2,"title":"Run corpus tests","text":"<p>Run the corpus conformance gate. The first run downloads a sparse checkout of the pinned veraPDF corpus:</p> <pre><code>just verapdf-corpus\n</code></pre>","path":["Contributing","Local set up"],"tags":[]},{"location":"contributing/local-set-up/#formatting-and-checks","level":2,"title":"Formatting and checks","text":"<p>Run formatting and lint checks with warnings treated as errors:</p> <pre><code>just fmt &amp;&amp; just lint\n</code></pre>","path":["Contributing","Local set up"],"tags":[]},{"location":"contributing/local-set-up/#documentation","level":2,"title":"Documentation","text":"<p>Serve the documentation locally with:</p> <pre><code>just preview\n</code></pre> <p>Then open <code>http://localhost:8000</code> to preview the documentation website.</p>","path":["Contributing","Local set up"],"tags":[]}]}